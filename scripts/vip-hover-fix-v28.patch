diff --git a/loader/blobio-loader.user.js b/loader/blobio-loader.user.js
index c966c8b..46ed59c 100755
--- a/loader/blobio-loader.user.js
+++ b/loader/blobio-loader.user.js
@@ -1,7 +1,7 @@
 // ==UserScript==
 // @name         Blobio Web Script Loader
 // @namespace    https://github.com/SkyViewBlobio/Blobgame.io-Web-Script
-// @version      0.1.45
+// @version      0.1.46
 // @description  Loads the Blobio modular extension bundle from GitHub.
 // @match        *://blobgame.io/*
 // @match        *://custom.client.blobgame.io/*
@@ -23,7 +23,7 @@
   'use strict';
 
   const LOG_PREFIX = '[Blobio]';
-  const VERSION = '0.1.45';
+  const VERSION = '0.1.46';
   const CUSTOM_CLIENT_HOST = 'custom.client.blobgame.io';
   const STORAGE_BRIDGE_SOURCE = 'BlobioExtensionStorageBridge';
   const CUSTOM_SKIN_ENABLED_KEY = 'blobio.customSkin.enabled';
diff --git a/package.json b/package.json
index 58954ec..6580dea 100755
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "blobio-web-script",
-  "version": "0.1.45",
+  "version": "0.1.46",
   "private": true,
   "type": "module",
   "repository": {
diff --git a/package-lock.json b/package-lock.json
index 5fd325b..4e2e0a0 100755
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "blobio-web-script",
-  "version": "0.1.45",
+  "version": "0.1.46",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "blobio-web-script",
-      "version": "0.1.45",
+      "version": "0.1.46",
       "devDependencies": {
         "esbuild": "^0.28.1"
       },
diff --git a/src/css/MenuFeatureStyles.js b/src/css/MenuFeatureStyles.js
index 7e6e256..188e66b 100755
--- a/src/css/MenuFeatureStyles.js
+++ b/src/css/MenuFeatureStyles.js
@@ -1005,36 +1005,46 @@ html.${className} .blobio-custom-skin-notice.is-error {
   box-shadow: 0 0 16px rgba(255, 49, 49, 0.3), inset 0 0 9px rgba(255, 89, 89, 0.18);
 }
 
-html.${className} .blobio-vip-plus-icon {
-  display: inline-block !important;
-  width: auto !important;
-  height: var(--blobio-vip-plus-size, 50px) !important;
-  max-width: 88px !important;
+html.${className} .blobio-vip-plus-slot {
+  display: inline-flex !important;
+  align-items: center !important;
+  justify-content: center !important;
+  flex: 0 0 auto !important;
   margin-left: 10px !important;
   vertical-align: middle !important;
-  object-fit: contain !important;
-  transform: scale(1);
-  transform-origin: center;
-  transition: transform 170ms ease, filter 170ms ease;
-  filter: drop-shadow(0 0 7px rgba(255, 196, 55, 0.28));
-  cursor: pointer;
-}
-
-html.${className} img[src*="mass_booster_web_trans.png"] {
+  line-height: 0 !important;
   transform: none !important;
   scale: 1 !important;
   transition: none !important;
   animation: none !important;
+  isolation: isolate;
+}
+
+html.${className} .blobio-vip-plus-icon {
+  display: block !important;
+  width: auto !important;
+  height: var(--blobio-vip-plus-size, 60px) !important;
+  max-width: 104px !important;
+  margin: 0 !important;
+  object-fit: contain !important;
+  transform: scale(1) !important;
+  scale: 1 !important;
+  transform-origin: center;
+  transition: transform 170ms ease, filter 170ms ease !important;
+  animation: none !important;
+  filter: drop-shadow(0 0 7px rgba(255, 196, 55, 0.28));
+  cursor: pointer;
+  pointer-events: auto !important;
 }
 
 html.${className} .blobio-vip-plus-icon:hover {
-  transform: scale(1.09);
+  transform: scale(1.09) !important;
   filter: drop-shadow(0 0 11px rgba(255, 204, 72, 0.48));
 }
 
 @media (prefers-reduced-motion: reduce) {
   html.${className} .blobio-vip-plus-icon {
-    transition: none;
+    transition: none !important;
   }
 }
 
diff --git a/src/features/MenuFeature.js b/src/features/MenuFeature.js
index f7df7cf..0c5c789 100755
--- a/src/features/MenuFeature.js
+++ b/src/features/MenuFeature.js
@@ -4,7 +4,7 @@ import { createBlobioStorage } from '../storage/BlobioStorage.js';
 const DEFAULT_CLASS_NAME = 'blobio-menu-enabled';
 const DEFAULT_STYLE_ID = 'blobio-menu-style';
 const DEFAULT_TOOLBAR_CLASS = 'blobio-menu-toolbar';
-const DEFAULT_EXTENSION_VERSION = '0.1.45';
+const DEFAULT_EXTENSION_VERSION = '0.1.46';
 const HIDDEN_CLASS = 'blobio-original-hidden';
 const PARTNER_LINK_MATCH = /iogames\.space|iogames\.live|io-games\.zone|silvergames\.com|crazygames\.com/i;
 const FAILED_VIRAL_FRAME_MATCH = /viral\.iogames\.space/i;
@@ -141,6 +141,7 @@ export class MenuFeature {
     this.documentClickHandler = null;
     this.keydownHandler = null;
     this.vipPlusIcon = null;
+    this.vipPlusSlot = null;
     this.vipPlusTarget = null;
   }
 
@@ -1845,12 +1846,21 @@ export class MenuFeature {
 
     const target = Array.from(this.document.querySelectorAll?.('img') || [])
       .find((image) => this.isMassBoosterImage(image));
+    const anchor = this.getVipPlusAnchor(target);
+    const host = anchor?.parentNode;
 
-    if (!target?.parentNode) {
+    if (!target || !host) {
       this.removeVipPlusIcon();
       return;
     }
 
+    let slot = this.vipPlusSlot;
+    if (!slot) {
+      slot = this.document.createElement('span');
+      slot.classList.add('blobio-vip-plus-slot');
+      this.vipPlusSlot = slot;
+    }
+
     let icon = this.vipPlusIcon;
     if (!icon) {
       icon = this.document.createElement('img');
@@ -1862,16 +1872,57 @@ export class MenuFeature {
       this.vipPlusIcon = icon;
     }
 
-    const parent = target.parentNode;
-    if (icon.parentNode !== parent || target.nextSibling !== icon) {
-      parent.insertBefore(icon, target.nextSibling || null);
+    if (icon.parentNode !== slot) {
+      slot.appendChild(icon);
+    }
+
+    const siblings = Array.from(host.children || []);
+    const anchorIndex = siblings.indexOf(anchor);
+    const referenceNode = anchorIndex >= 0 ? siblings[anchorIndex + 1] || null : null;
+
+    if (slot.parentNode !== host || referenceNode !== slot) {
+      host.insertBefore(slot, referenceNode);
     }
 
     this.vipPlusTarget = target;
     const height = Number(target.getBoundingClientRect?.().height) || Number(target.clientHeight) || Number(target.height) || 0;
     if (height >= 18 && height <= 120) {
-      this.setStyleProperty(icon, '--blobio-vip-plus-size', `${Math.round(height * 1.18)}px`);
+      this.setStyleProperty(icon, '--blobio-vip-plus-size', `${Math.round(height * 1.3)}px`);
+    }
+  }
+
+  getVipPlusAnchor(target) {
+    if (!target) {
+      return null;
     }
+
+    let anchor = target;
+    let parent = target.parentElement;
+
+    while (parent && parent !== this.document.body) {
+      const images = parent.querySelectorAll?.('img') || [];
+      if (images.length > 1) {
+        break;
+      }
+
+      const tagName = String(parent.tagName || '').toUpperCase();
+      const className = parent.className?.toString?.() || '';
+      const role = parent.getAttribute?.('role') || '';
+      const isIconControl =
+        tagName === 'BUTTON' ||
+        tagName === 'A' ||
+        role === 'button' ||
+        /booster|boost|icon-button/i.test(className);
+
+      if (!isIconControl) {
+        break;
+      }
+
+      anchor = parent;
+      parent = parent.parentElement;
+    }
+
+    return anchor;
   }
 
   isMassBoosterImage(image) {
@@ -1884,7 +1935,9 @@ export class MenuFeature {
   }
 
   removeVipPlusIcon() {
+    this.vipPlusSlot?.remove();
     this.vipPlusIcon?.remove();
+    this.vipPlusSlot = null;
     this.vipPlusIcon = null;
     this.vipPlusTarget = null;
   }
@@ -2443,6 +2496,7 @@ export class MenuFeature {
         classList?.contains('blobio-custom-skin-tab') ||
         classList?.contains('blobio-custom-skin-panel') ||
         classList?.contains('blobio-custom-skin') ||
+        classList?.contains('blobio-vip-plus-slot') ||
         classList?.contains('blobio-vip-plus-icon')
       ) {
         return true;
diff --git a/tests/loader.test.js b/tests/loader.test.js
index d7e6762..60e6344 100755
--- a/tests/loader.test.js
+++ b/tests/loader.test.js
@@ -114,10 +114,10 @@ function createLoaderContext({ host, gmValues = new Map() }) {
   return { context, localValues, requests, valueListeners, gmValues };
 }
 
-test('Tampermonkey loader targets both Blobgame hosts and loads version 0.1.45', () => {
+test('Tampermonkey loader targets both Blobgame hosts and loads version 0.1.46', () => {
   assert.match(loader, /\/\/ @match\s+\*:\/\/blobgame\.io\/\*/);
   assert.match(loader, /\/\/ @match\s+\*:\/\/custom\.client\.blobgame\.io\/\*/);
-  assert.match(loader, /\/\/ @version\s+0\.1\.45/);
+  assert.match(loader, /\/\/ @version\s+0\.1\.46/);
   assert.match(loader, /\/\/ @run-at\s+document-start/);
   assert.match(loader, /\/\/ @grant\s+unsafeWindow/);
   assert.match(loader, /\/\/ @grant\s+GM_xmlhttpRequest/);
@@ -139,7 +139,7 @@ test('custom client installs the direct carrier replacement before loading the b
   assert.equal(result.context.__blobioCarrierSkinReplacerInstalled, true);
   assert.equal(typeof result.context.__blobioCarrierSkinRefresh, 'function');
   assert.equal(typeof result.context.__blobioCarrierSkinStatus, 'function');
-  assert.match(result.requests[0].url, /blobio-extension\.bundle\.js\?v=0\.1\.45/);
+  assert.match(result.requests[0].url, /blobio-extension\.bundle\.js\?v=0\.1\.46/);
 });
 
 test('carrier hook replaces image, setAttribute, XHR, and fetch URLs immediately', async () => {
diff --git a/tests/menu-feature.test.js b/tests/menu-feature.test.js
index c97e9a7..2589317 100755
--- a/tests/menu-feature.test.js
+++ b/tests/menu-feature.test.js
@@ -859,34 +859,50 @@ test('MenuFeature splits logged-in username into staggered animated letters', ()
   feature.destroy();
 });
 
-test('MenuFeature places the VIP+ icon beside the mass booster and removes it on cleanup', () => {
+test('MenuFeature isolates the VIP+ icon from the mass booster hover area and removes it on cleanup', () => {
   const document = createFakeDocument();
   addReplayButton(document);
 
   const boosterRow = document.createElement('div');
+  const massControl = document.createElement('button');
+  massControl.classList.add('mass-booster-button');
   const massBooster = document.createElement('img');
   massBooster.setAttribute('src', 'assets/images/mass_booster_web_trans.png');
   massBooster.getBoundingClientRect = () => ({ height: 46 });
-  boosterRow.appendChild(massBooster);
+  massControl.appendChild(massBooster);
+
+  const xpControl = document.createElement('button');
+  const xpBooster = document.createElement('img');
+  xpBooster.setAttribute('src', 'assets/images/xp_booster_web_trans.png');
+  xpControl.appendChild(xpBooster);
+
+  boosterRow.append(massControl, xpControl);
   document.body.appendChild(boosterRow);
 
   const feature = new MenuFeature({ document, assets });
   feature.start();
 
+  const vipSlot = document.querySelector('.blobio-vip-plus-slot');
   const vipIcon = document.querySelector('.blobio-vip-plus-icon');
   const style = document.getElementById('blobio-menu-style').textContent;
 
+  assert.notEqual(vipSlot, null);
   assert.notEqual(vipIcon, null);
-  assert.equal(vipIcon.parentNode, boosterRow);
-  assert.equal(boosterRow.children.indexOf(vipIcon), boosterRow.children.indexOf(massBooster) + 1);
+  assert.equal(vipSlot.parentNode, boosterRow);
+  assert.equal(boosterRow.children.indexOf(vipSlot), boosterRow.children.indexOf(massControl) + 1);
+  assert.equal(vipIcon.parentNode, vipSlot);
+  assert.equal(massControl.contains(vipIcon), false);
+  assert.equal(xpControl.contains(vipIcon), false);
   assert.equal(vipIcon.getAttribute('src'), assets.vipPlusIcon);
   assert.equal(vipIcon.getAttribute('alt'), 'VIP+');
-  assert.equal(vipIcon.style['--blobio-vip-plus-size'], '54px');
-  assert.match(style, /\.blobio-vip-plus-icon\s*\{[\s\S]*transition: transform 170ms ease, filter 170ms ease;/);
-  assert.match(style, /\.blobio-vip-plus-icon:hover\s*\{[\s\S]*transform: scale\(1\.09\);/);
-  assert.match(style, /img\[src\*=\"mass_booster_web_trans\.png\"\][\s\S]*transform: none !important;[\s\S]*scale: 1 !important;[\s\S]*transition: none !important;/);
+  assert.equal(vipIcon.style['--blobio-vip-plus-size'], '60px');
+  assert.match(style, /\.blobio-vip-plus-slot\s*\{[\s\S]*transform: none !important;[\s\S]*transition: none !important;/);
+  assert.match(style, /\.blobio-vip-plus-icon\s*\{[\s\S]*transform: scale\(1\) !important;[\s\S]*transition: transform 170ms ease, filter 170ms ease !important;/);
+  assert.match(style, /\.blobio-vip-plus-icon:hover\s*\{[\s\S]*transform: scale\(1\.09\) !important;/);
+  assert.doesNotMatch(style, /img\[src\*=\"mass_booster_web_trans\.png\"\]/);
 
   feature.destroy();
+  assert.equal(document.querySelector('.blobio-vip-plus-slot'), null);
   assert.equal(document.querySelector('.blobio-vip-plus-icon'), null);
 });
 
